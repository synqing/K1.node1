# Components

Component specs, variants, states, and accessibility. Link to code usage.
- One file per component with metadata frontmatter (`id`, `name`, `status`, `version`, `tags`).
- Use `TitleCase` for component names; keep variants documented within the same file.
