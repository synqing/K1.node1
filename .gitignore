# Ignore everything by default, only keep core sources and mem0 integration tracked
/*
# Keep root tracked files
!.gitignore

# Unignore firmware and webapp directories, then their contents
!firmware/
!firmware/**
!webapp/
!webapp/**

# Track documentation (analysis, planning, implementation, reports)
!docs/
!docs/**

# Track tools (utilities and fixtures)
!tools/
!tools/**

# Track GitHub workflows and local hooks
!.github/
!.github/**
!.githooks/
!.githooks/**

# Mem0 local workspace is ignored (ephemeral/local-only)
.mem0_local/

# Typical build/caches to ignore inside tracked trees
firmware/.pio/
firmware/.pio_home/
firmware/.platformio/
firmware/.platformio/**
webapp/node_modules/
webapp/dist/
webapp/.vite/

# OS cruft
*.DS_Store
docs/.DS_Store

.config/
!README.md
!memory-proxy/
!memory-proxy/**
